import SwiftUI

enum StoryStyle {
    static let pageBackground = AppTheme.backgroundTop
    static let menuBackground = AppTheme.backgroundTop
    static let choiceBackground = AppTheme.gold
    static let textColor = AppTheme.warmText
    static let choiceCornerRadius: CGFloat = AppTheme.buttonRadius
    static let choiceWidth: CGFloat? = nil
    static let choiceHeight: CGFloat? = nil
    static let pagePadding: CGFloat = AppTheme.screenPadding
}
